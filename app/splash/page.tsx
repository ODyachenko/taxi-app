import Logo from '@/UI/Btn/Logo';
import React, { FC } from 'react';
import './styles.scss';

const page: FC = () => {
  return (
    <div className="splash">
      <Logo color="#fff" />
    </div>
  );
};

export default page;
